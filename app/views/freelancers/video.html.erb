<div class="container">
    <div class="index">
        <div class="row">
            <div class="col-3">
                <div class="category">
                    <img src="<%= asset_path( 'video-index.png' ) %>" /><br>
                    <img src="<%= asset_path( 'login-bar1.png' ) %>" /><br>
                    <img src="<%= asset_path( 'category.png' ) %>" /><br>
                    <img src="<%= asset_path( 'login-bar3.png' ) %>" /><br>
                </div>
            </div>
            
            <div class="col-8">
                <div>
                    <img src="<%= asset_path( 'video-category.png' ) %>" /><br>
                </div>
                <div>
                    <% @videos.each do |video| %>
                        <a href="video/show/<%= video.id %>"><img src="<%= video.locallage.url %>"></a>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>